# Simple Quant & Stock Playground

**Simple Quant** 是一个基于 Python 的事件驱动量化交易回测框架，集成了自主 AI 代理（Agent），能够自动生成、测试和优化交易策略。本项目旨在提供一个稳健的量化研究平台，强调策略的稳定性和抗过拟合能力。

---

## 🌟 核心特性

### 1. 🤖 AI 策略代理 (Autonomous Agent)
- **自主编程**：基于 LLM（大语言模型）自动编写 Python 策略代码。
- **自我修复**：自动捕获回测报错，并将错误反馈给 LLM 进行代码修复。
- **自动验证**：生成的策略会自动在训练集上进行验证，并保存回测报告。
- **降级模式**：若 LLM 服务不可用，系统会自动降级到 Mock 模式演示流程。

### 2. 🛡️ 稳健回测引擎 (Robust Portfolio)
- **真实资金风控**：交易前实时检查现金余额，杜绝“无限资金”作弊。
- **高级评价指标**：
  - **Stability Score (稳健性得分)**：综合夏普比率和最大回撤的复合指标。
  - **Calmar Ratio**：年化收益 / 最大回撤。
  - **Sortino Ratio**：下行风险调整收益。
- **精准撮合**：支持模拟实盘的订单撮合逻辑（处理停牌、涨跌停等）。

### 3. 🔍 策略探索 (Exploration & Optimization)
- **参数网格搜索**：自动遍历策略参数（如均线周期）。
- **样本外验证 (Walk-Forward)**：
  - **训练集 (In-Sample)**：2020-2023 年数据用于筛选。
  - **测试集 (Out-of-Sample)**：2024 年数据用于验证。
  - **过拟合检测**：自动标记在测试集表现大幅下滑的策略为 `FAIL`。

### 4. 📊 专业数据源 (Data Feeds)
- **TDX (通达信)**：集成 `pytdx` 接口，高速获取 A 股（沪深）5 年以上的高频/日线数据。
- **CSV 支持**：兼容 Yahoo Finance 格式的 CSV 数据导入。

---

## 🚀 快速开始

### 1. 环境准备

```bash
# 创建并激活虚拟环境 (可选)
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate   # Windows

# 安装依赖
pip install -r requirements.txt
# 推荐安装 pytdx 获取高质量数据
pip install pytdx
```

### 2. 配置 AI (可选)

复制并编辑 `.env` 文件以配置 LLM 服务（如 LocalAI, vLLM, OpenAI, DeepSeek）：

```env
MODEL_URL=http://localhost:11434/v1/chat/completions
MODEL_API_KEY=your-api-key
MODEL_NAME=deepseek-coder
```

### 3. 获取数据

下载 A 股（如 茅台、平安）过去 5 年的历史数据：

```bash
python stock_playground/scripts/fetch_tdx_data.py
```

### 4. 运行 AI 策略代理

让 AI 帮你写一个策略：

```bash
python stock_playground/agent/manager.py "写一个基于 RSI 超买超卖的均值回归策略"
```
*生成的策略将保存在 `stock_playground/generated_strategies/` 目录下。*

### 5. 探索最佳参数

对现有策略（如双均线）进行参数挖掘和稳健性测试：

```bash
python stock_playground/explore_strategies.py
```

### 6. 通用回测

对本地所有数据文件运行基准策略回测：

```bash
python stock_playground/run_universal_backtest.py
```

---

## 📂 项目结构

```
playground/
├── stock_playground/
│   ├── agent/                 # AI 代理核心模块 (Manager, Executor, Client)
│   ├── simple_quant/          # 量化框架核心
│   │   ├── data/              # 数据处理 (TDX, CSV)
│   │   ├── strategy/          # 策略基类与标准策略
│   │   ├── portfolio/         # 投资组合与资金管理 (RobustPortfolio)
│   │   ├── execution/         # 订单执行模拟
│   │   └── engine.py          # 事件驱动回测引擎
│   ├── scripts/               # 数据获取脚本
│   ├── generated_strategies/  # AI 生成的策略仓库
│   ├── explore_strategies.py  # 参数探索脚本
│   └── run_universal_backtest.py # 通用回测入口
├── data/                      # 历史行情数据 (.csv)
├── docs/                      # 文档
├── .env                       # 环境变量配置
└── requirements.txt           # 项目依赖
```

## 📈 关键指标说明

| 指标 | 理想范围 | 说明 |
| :--- | :--- | :--- |
| **Stability Score** | > 0.5 | 我们自研的稳健性得分，越高越好。 |
| **Sharpe Ratio** | > 1.0 | 承担单位风险获得的超额回报。 |
| **Max Drawdown** | < 15% | 历史最大回撤，越低越好。 |
| **Calmar Ratio** | > 1.0 | 收益回撤比，衡量策略性价比。 |

---
*Created by Antigravity Agent*
